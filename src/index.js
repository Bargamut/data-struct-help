const dataStructures = [
	// Массив / Список
	// порядок, ключи уникальны, значения не уникальны;
	// ключи - индексы, данные любого типа
	`array`,
	// Словарь
	// без порядка, ключи уникальны, значения не уникальны;
	// ключи - только строки, данные любого типа
	`object`,
	// ключи и значения любого типа
	`map`,
	`weak-map`,
	// без порядка, значения уникальны, любого типа
	`set`,
	`weak-set`
];

document.addEventListener(`DOMContentLoaded`, () => {
	console.log(`Hello World!!!`);
});